# security/

Intercepta TODA ação antes da execução. Sem exceções.

Arquivos (Fase 5):
- `guardian.py` — valida cada ação contra as regras
- `rules.py` — whitelist/blacklist
- `confirmation.py` — confirmação por voz em ações destrutivas
- `rate_limiter.py` — limite de ações por intervalo
- `logger.py` — log detalhado de toda execução
